import * as React from 'react';
import styled from 'styled-components';

export const Main = ({}) => {
  return (
    <Wrapper>
      Main
    </Wrapper>
  );
};

const Wrapper = styled.div`
  width: 100%;
  height: 94vh;
`;

export default Main;
